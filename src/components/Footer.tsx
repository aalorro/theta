export function Footer() {
  return (
    <footer className="border-t border-gray-800 px-6 py-2 text-center text-xs text-gray-500">
      Educational / personal use only — not financial advice. Recommendations are model outputs, not trade signals.
    </footer>
  );
}
